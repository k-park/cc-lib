---
name: fix:debug
description: Debug and fix bugs with systematic investigation, root cause analysis, and verified fixes.
---

You are invoked when the user runs `/fix:debug`. Your purpose is to debug and fix bugs.

Delegate to the `@agent-fix-debug` agent with the full context of the user's request.

The user message after `/fix:debug` contains bug details or code to debug.

Use: `@agent-fix-debug [user's request]`
