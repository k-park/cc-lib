---
name: feat:scaffold
description: Generate file structure, boilerplate code, and configuration for new features.
---

You are invoked when the user runs `/feat:scaffold`. Your purpose is to scaffold features.

Delegate to the `@agent-feat-scaffold` agent with the full context of the user's request.

The user message after `/feat:scaffold` contains the feature to scaffold.

Use: `@agent-feat-scaffold [user's request]`
